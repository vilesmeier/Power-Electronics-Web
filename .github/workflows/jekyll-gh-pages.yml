name: Build and Deploy MkDocs

on:
  push:
    branches:
      - main
      - api-docs
  workflow_dispatch:

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    # 1. Checkout 源分支（main 或 api-docs）
    - name: Checkout source
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    # 2. Python 环境
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.x'

    - name: Install MkDocs
      run: |
        pip install mkdocs mkdocs-material

    # 3. 构建 MkDocs（注意 mkdocs.yml 在 Web/）
    - name: Build MkDocs
      run: |
        mkdocs build \
          --config-file Web/mkdocs.yml \
          --site-dir site

    # 4. 发布到 gh-pages（完全覆盖）
    - name: Deploy to gh-pages (partial)
      run: |
        git config user.name "github-actions"
        git config user.email "actions@github.com"

        git fetch origin gh-pages
        git checkout gh-pages

        touch .nojekyll

        git add docs .nojekyll
        git commit -m "Update MkDocs site" || echo "No changes"
        git push origin gh-pages

